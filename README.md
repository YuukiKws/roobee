# Roobee

日本語のウェブページを読んでいるだけで、単語に英単語のルビが表示される
Chrome拡張機能です。単語帳アプリのように意識して開かなくても、日常的に
英単語に触れる機会を増やすことをコンセプトにしています。

- 日本語ページ内の名詞・動詞・形容詞に、対応する英単語を上付きで表示
- クリックすると、英単語・カタカナ読み・類義語・(動詞の場合は)過去形/過去分詞を表示
- ラベル表示は「英語」「カタカナ」で切り替え可能
- 単語ごとに表示ON/OFFを個別設定可能(検索機能付き)
- 現在**3,000語**収録、継続して語彙を拡充中

## インストール方法

Kuromoji.js本体と形態素解析用の辞書データは、このリポジトリに同梱済みです。
別途ダウンロードする必要はありません。

1. 右上の「Code」→「Download ZIP」でこのリポジトリをダウンロードする(または`git clone`する)
2. 解凍してできた`roobee-main`フォルダ(そのままの名前でOK)を用意する
3. Chromeで `chrome://extensions` を開く
4. 右上の「デベロッパーモード」をONにする
5. 「パッケージ化されていない拡張機能を読み込む」から、`roobee-main`フォルダを選択する

## 使い方

1. ツールバーのアイコンをクリックし、「ルビ表示」をONにする
2. **ページを再読み込みする**(ON/OFFの切り替えはページ再読み込み後に反映されます)
3. 日本語のニュースサイトやブログなどを開くと、単語にラベルが表示されます
4. オレンジ色の単語をクリックすると、詳細(カタカナ読み・類義語・過去形など)がポップアップ表示されます
5. ツールバーの「単語ごとの詳細設定を開く」から、特定の単語だけ表示をOFFにすることも可能です

## 制限事項

- 形態素解析(Kuromoji + IPADIC)の都合上、「遊べる」のような可能形や、一部の
  活用形は、元の動詞(「遊ぶ」)とは別の語として個別に登録しないと反応しません。
  気になる語があれば、随時追加していきます。
- な形容詞・い形容詞の一部には対応していますが、副詞は基本的に対象外です
  (「擬態語+する」型の語は例外的に対応しています)。

## ライセンス

同梱している Kuromoji.js および IPADIC辞書データのライセンス表記は
`THIRD_PARTY_LICENSES.txt` を参照してください。

## アップデート告知機能について(開発者向けメモ)

新しいバージョンをリリースしたら、リポジトリ直下の`latest.json`を以下の形式で更新してください。
ユーザーがポップアップを開いた時に、この内容と手元のバージョンを比較して、古ければお知らせバナーが表示されます。

```json
{
  "latest": "0.46",
  "url": "https://github.com/YuukiKws/roobee"
}
```

`latest`は新しいバージョン番号、`url`はダウンロード先(リポジトリのトップページでOK)。

## アップデート履歴

### v0.46
- リポジトリ構成をフラット化(`roobee_ext`のようなサブフォルダを介さず、リポジトリ直下に直接配置する形に変更)。GitHubの「Download ZIP」でそのまま`roobee-main`フォルダを読み込める構成に。
- READMEのインストール手順を修正: 公開版はKuromoji.js・辞書データを同梱済みのため、「別途ダウンロードしてください」という古い記述を削除。

### v0.45
- アップデート告知機能を追加。GitHubリポジトリ直下の`latest.json`と手元のバージョンを比較し、新しいバージョンがあればポップアップ上部にお知らせバナーを表示します。

### v0.44
- アプリ名を「Roobee」に変更
- 公開用READMEを整備(詳細な開発ログは `DEVLOG.md` に移動)

### v0.40〜v0.43
- 辞書を3,000語に拡張(基本語彙、複合動詞の名詞形、実記事から見つけた語彙、
  日常会話のイディオム・擬音語まで収録)
- 「擬態語+する」型の動詞(びっくりする、わくわくする、など)が複合語として
  正しく認識されるよう修正
- ルビ表示の初期状態をOFFに変更
- OFF状態ではDOM書き換え自体を行わないよう修正(以前はCSSで非表示にしてるだけでした)

### v0.33〜v0.39
- い形容詞に対応(新しい、大きい、楽しい、など)
- 実際の記事(ゲームレビュー、アニメコラムなど)を検証し、頻出語の抜けを継続的に補完
- 「する動詞」に対応する裸の名詞(例:「開発する」に対する「開発」)を132語追加

### v0.25〜v0.32
- 2,000語到達
- CSVパーサーをヘッダー駆動方式に変更(将来の列追加に柔軟に対応)
- 辞書CSVを`dict_words/`フォルダにまとめ、`dict2000.csv`として1ファイルに統合
- Kuromojiによる複合語の最長一致マッチングを実装(「質問する」「数年」などの
  複合語が正しく認識されるように)

### v0.14〜v0.24
- 動詞に過去形・過去分詞を追加、吹き出しに表示
- 単語ごとの詳細設定画面(検索+個別ON/OFF)を追加
- 700語まで拡張

### v0.1〜v0.13
- プロトタイプ開発開始。単純な文字列マッチングから、Kuromojiによる形態素解析へ移行
- ルビのデザイン、吹き出しのUI、外部リンク化などを継続的に改善
- アプリアイコンを設定
