# Rooby_JP

日本語ページ内の動詞・名詞(辞書に登録されている400語)に、対応する英単語をルビのように表示し、
クリックするとカタカナ読み・類義語の吹き出しが出るChrome拡張のプロトタイプです。

## v0.43での変更点

- ON/OFFトグルの直下にある注意書きを、「On/Offを変更した場合はページをリロードしてください。」に変更しました(v0.42の変更で「即反映されます」という従来の案内が実態と合わなくなっていたため)。

## v0.42での変更点

- **OFF状態でページを開いた場合、形態素解析・DOM書き換え自体を行わないように変更**しました。これまではOFFでもルビ用のタグ(`.rbv-jp-base`など)がDOMに挿入され、CSSで見た目だけ非表示にしていましたが、今回からはOFFのページではタグそのものが一切挿入されません。
  - **注意**: ONの状態でページを開いた後、ツールバーから初めてONに切り替えても、その場では何も表示されません(タグの挿入処理はページ読み込み時にしか実行されないため)。ONの状態でページを再読み込みした時に初めて反映されます。既にタグが挿入されたページをOFFにした場合は、従来通りCSSで非表示になります(DOMからの削除はしません)。

## v0.41での変更点

- 初期状態(初回インストール時)のルビ表示を、ON→**OFF**に変更しました。`content.js`と`popup.js`のデフォルト値、および`popup.html`のチェックボックスの初期表示を修正しています。

## v0.40での変更点 🎉 3000語到達

- **技術修正: 複合語マッチングの品詞対象に「副詞」を追加**しました。「びっくりする」「わくわくする」のような「擬態語+する」型の語は、Kuromojiが擬態語部分を「副詞」として認識するため、これまでの実装(名詞・動詞・形容詞のみ対象)では複合語として正しくマッチしていなかった可能性があります。単体トークンの判定基準は変更していないため、通常の副詞(単体)が新たにルビ化されることはありません。
- 200語を追加。判断・評価系な形容詞、性格・気質、ビジネス財務用語、モダンテクノロジー・SNS用語(いいね・シェア・人工知能など)、旅行・食事・健康関連語、整理整頓系の動詞、能力・気質を表す語、そして**擬音語・擬態語+する型の動詞23語**(イライラする・ハラハラする・モヤモヤする・ゴロゴロする・バタバタするなど)を含みます。
- 辞書は**合計3000語**になりました。
- 作成時の自己チェックで1件の重複を発見・修正済み(「収益」の`earnings`が既存の「利益」と重複)。

## v0.39での変更点 🎉 2800語到達

- 日常会話のイディオム・慣用句を中心に100語を追加。
  - 四字熟語(一石二鳥・七転び八起き・臨機応変・一期一会など)15語
  - 気分・状態を表すな形容詞(大丈夫・面倒・無理・迷惑・残念・大変・普通・微妙・最悪・最高など)32語
  - 性格・様子を表すな形容詞(当然・贅沢・地味・派手・器用・大切・冷静・積極的など)19語
  - カジュアルな動詞(慌てる・びっくりする・がっかりする・遠慮する・気にする・困るなど)16語
  - カジュアルな形容詞(すごい・やばい・もったいない・しんどい・きつい・うざいなど)7語
  - その他の口語表現(本気・油断・根気・気合・天然など)11語
- 辞書は**合計2800語**になりました。
- 作成時の自己チェックで1件の重複を発見・修正済み(「理想的」の`ideal`が既存の「理想」と重複)。

## v0.38での変更点 🎉 2700語到達

- 使用頻度が高いと判断した語123語を追加(場合・関係・便利・不便・明確・曖昧・優先・過程・実際・真実・検証など、日常会話からビジネス・ニュース文まで幅広く使われる抽象語・関係性を表す語)。
- 辞書は**合計2700語**になりました。重複なしを確認済みです。

## v0.37での変更点

- 新規20語を追加(計測・最適化・読み込み・重要・分野・指標・表示・主要・予期・予測・操作・領域・数値化・良好・累積・動的・静的・視覚的・測定・安定・不安定)。技術・分析系の抽象語が中心。
- 辞書は**合計2577語**になりました。

## v0.36での変更点

- 「経緯」の英単語を`background circumstances`から`history`に変更(ユーザー指摘により、こちらの方が適切と判断)。ただし`history`は既に「歴史」の英単語だったため、「歴史」の方を`chronicle`(類義語にhistoryを残す)に変更して重複を解消しました。
- 新規16語を追加(機会・体制・意見・投稿・表情・耐久・強化・体験・装着・方針・制作・簡単・仕掛け・設計・奥行き・機能)。指定された17語のうち「経験」は既存(283番)だったため除外。
- 辞書は**合計2556語**になりました。

## v0.35での変更点

- ユーザー指定の記事(アニメコラム、4Gamerゲーム記事)から見つかった不足語62件を追加。うち1件(「姿勢」)は既存と重複してたため除外。
- 「実は」「何度も」は**副詞**のため、現在の品詞対応(名詞・動詞・形容詞)では表示対象外。追加を見送り。
- 「可愛」は既存の「かわいい」(ひらがな表記)とは別の見出し語になる可能性が高いため、漢字表記「可愛い」を別エントリとして追加。**表記ゆれ(漢字/ひらがな)は今後も同様の見落としが起きうる**点を記録。
- 辞書は**合計2540語**になりました。

## v0.34での変更点

- 実際の記事3本(キングダムハーツ4インタビュー再確認、『FFレゾナンス』体験版レビュー、『オービタルズ』レビュー)を読み、ゲームレビュー記事で頻出する語彙の穴24語を追加(圧巻・迫力・手触り・育成・絵柄・出来栄え・予感・傑作など)。
- 辞書は**合計2478語**になりました。重複なしを確認済みです。

## v0.33での変更点(形容詞対応)

- **い形容詞をルビ化・クリック可能化の対象に追加しました。** `content.js`の品詞フィルタに`形容詞`を追加(単体マッチ・複合語マッチ両方に反映)。な形容詞(多感な、など)はIPADIC上では名詞のサブカテゴリとして扱われることが多く、対応は別途要検討です。
- 高頻度ない形容詞69語(新しい・古い・大きい・小さい・良い・悪い・楽しい・悲しい・かわいい・美味しいなど)を追加。
- 実際の記事3本(キングダムハーツ4インタビュー、攻殻機動隊コラム、既出のFamitsu/GameMakers記事の続き確認)を読み、見つかった語彙の穴33語(探索・移動・到着・調整・発売・順調・限界・歴史・発見など)を追加。
- 辞書は**合計2454語**になりました。
- 作成時の自己チェックで1件の重複を発見・修正済み(「安心」が既存の1114番と重複)。

## v0.32での変更点

- 辞書を2353語に拡張(`dict4000.csv`に221語分を追加)。
  - 2133〜2232: 実地検証(Famitsu記事)で見つかった語彙の穴を優先的に補完(家族の続柄、学校関連、抽象概念語、素材、感情語)
  - 2233〜2332: ゲーム業界の役職・ゲーム関連用語・メディア/取材用語・感想表現・創造性を表す語・物語構成用語(2件目の実地検証記事を踏まえた内容)
  - 2333〜2353: ニュース・コラムで高頻度な機能語(可能・不可能・有効・無効・従来・使用・制御・対象・該当など)を優先追加。実際の記事(gamemakers.jp)で見つかった語彙の穴を反映
- 作成時の自己チェックで2件の重複を発見・修正済み(「進行する」の`progress`が既存の「途中経過」と重複、「追加」の`addition`が既存の「足し算」と重複)。
- **形容詞(新しい・古いなど)は今回見送り**。現在の実装は品詞フィルタが「名詞・動詞」のみのため、形容詞対応には別途コード変更(POS対象の拡張)が必要。優先度を下げて後日対応予定。

## v0.31での変更点

- `THIRD_PARTY_LICENSES.txt`を新規追加。Kuromoji.js(Apache License 2.0)とIPADIC辞書データ(BSDスタイルライセンス)の著作権表示を記載しています。どちらも再配布が認められているライセンスですが、著作権表示を残す必要があるため追加しました。
- 辞書を`dict4000.csv`(2133〜2232語、100語)で拡張し、**合計2232語**になりました。今回は実際の記事(Famitsu)で検証した際に見つかった語彙の穴(兄・小学生・人格・形成・時期・衝撃・物語・中心・進行・感覚など)を優先的に埋め、関連して家族の続柄(姉・弟・妹)、学校関連語彙、抽象的な概念語、素材名、感情語を追加しています。
- 作成時の自己チェックで1件の重複を発見・修正済み(「進行する」の`progress`が既存の「途中経過」と重複)。

## v0.30での変更点

- 統合CSVファイルを`dict2132.csv`から**`dict2000.csv`にリネーム**しました(中身は変更なく2132語のまま。「2000語+「する動詞」の裸の名詞」という節目を表す名前にする、という意図です)。
- 今後2133語以降を追加する際は、**新しいファイル`dict4000.csv`として追加していく**方針で進めます(`dict2000.csv`はこのまま2132語で固定し、追記はしない想定)。

## v0.29での変更点

- `dict_words/`内の21個の小分けCSV(`dict_001to100.csv`〜`dict_2001to2132.csv`)を、**`dict2132.csv`という1つのファイルに統合**しました。`dict_manifest.js`も統合ファイル1つだけを参照する形に変更しています。
  - ヘッダーはすべて共通(優先度,日本語,品詞,カテゴリ,英単語,カタカナ読み,過去形,過去分詞,類義語)だったため、単純に中身を連結する形で統合できました。重複や欠落がないことも確認済みです。
  - 今後2133語以降を追加する際は、`dict4000.csv`のように新しいファイルを別途追加していく想定です(`dict_loader.js`のヘッダー駆動パーサーはファイル数に依存しないため、この構成変更でもコード側の修正は不要でした)。

## v0.28での変更点

- 「◯◯する」型の動詞のうち、対応する裸の名詞(「◯◯」)が未登録だったもの133件(「関する」は例外として除外)を洗い出し、**132件を新規登録**しました(`dict_2001to2132.csv`)。これで辞書は**2132語**になりました。
  - 例:「節約する→save」に対して「節約→thrift」を追加、「開発する→develop」に対して「開発→development」を追加、など
  - 動詞と名詞で英単語が同じ文字列にならないよう(例:「交換する」がexchangeなら「交換」はswapにする、など)注意しながら割り当てています
- 作成時の自己チェックで1件の重複を発見・修正済み(「発明」の`invention`が既存の「発明品」と重複)。

## v0.27での変更点(v0.26の記述を訂正)

- v0.26で「日本語とカタカナ読みが一致する場合、吹き出しのカナ行を非表示にする」と実装しましたが、正しくは**「そもそもルビ表示・クリック可能化の対象から完全に除外する」**という仕様でした。修正しました。
  - 「日本語」と「カタカナ読み」が完全一致するエントリ(例:「シングル」)は、辞書引き用のマップを構築する段階で除外されるようになり、単体マッチ・複合語マッチ(v0.19の最長一致ロジック)のどちらにも一切引っかからなくなります。文中のその単語はルビも付かず、クリックもできない(普通の文章として扱われる)状態になります。
  - この除外は自動判定で、設定画面(手動除外リスト)とは別の仕組みです。該当する単語は設定画面の一覧には引き続き表示されますが、チェックのON/OFFに関わらずページ上には反映されません(この点は将来的に分かりやすくする余地があります)。

## v0.26での変更点

- 吹き出しのデザインを変更: **日本語の見出し語を英単語と同じ行に、大きめの文字で表示**するようにしました(以前は日本語を吹き出しから外していましたが、デザイン方針の変更により復活させています)。表示する日本語は辞書の見出し語(辞書形)であり、文中の活用形(例:「話した」)とは異なる場合があります。
- **「日本語」と「カタカナ読み」が完全一致する場合、カナ行を表示しないように変更**しました(例:「シングル」のようなカタカナ語で、読み仮名を表示しても意味がないケースに対応)。CSV側は変更せず、表示側のロジックで判定しています。
- 複合動詞の追加登録タスクについて、方針を訂正しました。「質問する」「質問」は既に両方登録済みであることを確認済みです。実際に必要なのは、**「◯◯する」型の動詞は登録されているが対応する裸の名詞が無い(またはその逆の)組み合わせを2000語全体から洗い出して埋める**作業です。この監査作業は次回以降に着手します。

## v0.25での変更点 🎉 2000語到達

- 辞書を**2000語**に拡張しました。当初目標としていた第一段階の語彙数に到達しました。
  - `dict_1801to1900.csv`: 鳥類・昆虫・海洋生物、医療の診療科、日本の代表的な料理(寿司・ラーメン・天ぷらなど)、音楽ジャンル、地形用語、政治体制用語、宇宙・天文用語、衣類の部位、リーダーシップ関連の役職
  - `dict_1901to2000.csv`: 調理動詞の追加、順序・時代を表す語、モチベーション関連の感情語、乗り物の追加、社会的役割、花・木の種類、掃除用品、単位・計量語、光や存在に関する動詞
- 作成時の自己チェックで3件の重複を発見・修正済み(「フォーク」の見出し語が既存の調理器具と重複、「襟」の`collar`が既存の「首輪」と重複、入力ミスで括弧付きの見出し語になっていた箇所を修正)。
- **次のステップ**: ここからは、既存の2000語に含まれる名詞(例:「質問」)に対して、対応する複合動詞(例:「質問する」)を追加登録していく作業を予定しています(v0.19で対応した複合語の最長一致ロジックにより、これらが正しく機能する見込みです)。

## v0.24での変更点

- 辞書を1800語に拡張しました。
  - `dict_1601to1700.csv`: 学校区分(幼稚園〜大学院)、体の内臓、日本の年中行事、天文現象、建築用語、学問分野、行政機関、スポーツ用語、プログラミング用語、怪我・体調不良
  - `dict_1701to1800.csv`: 料理の材料(スパイス・調味料など)、交通機関の職業、金融・為替、法律・訴訟関連、理科実験器具、農業・畜産、爬虫類・農場の動物、リビング/寝室の家具、あいさつ・言語関連、数量表現
- 作成時の自己チェックで1件の重複を発見・修正済み(「基礎」が既存の1142番と完全に同じ見出し語で重複)。

## v0.23での変更点

- 辞書を1600語に拡張しました。
  - `dict_1401to1500.csv`: 旅行・学校科目・防災、美容、恋愛関連、財務用語、物流、法律・契約、修理/保証、会議用語
  - `dict_1501to1600.csv`: ゲーム用語、キッチン家電、災害・体調不良の天候関連、起業/スタートアップ、ファッション業界、音楽/エンタメ、不動産、料理ジャンル・食事制限、日常ルーティン動詞、抽象・哲学的概念
- 作成時の自己チェックで1件の重複を発見・修正済み(「利益」の`profit`が既存の「儲ける」と重複)。

## v0.22での変更点

- 辞書を1400語に拡張しました。
  - `dict_1201to1300.csv`: 料理動詞(炒める・蒸す・揚げるなど)、特定の食材・デザート、自然現象(潮・波・洞窟など)、性格を表す名詞(情熱・忍耐・寛容など)、工具、衣類、交通・自動車用語、コンピューター用語、空・天候
  - `dict_1301to1400.csv`: DIY/建築動詞、昆虫・海洋生物、図形、数学用語、アート用語、メディア・出版用語、郵便、浴室用品、衣類アクセサリー、感情表現の動詞(元気づける・落ち着くなど)
- 作成時の自己チェックで1件の重複を発見・修正済み(「微笑む」の`smile`が既存の「笑顔」と重複)。

## v0.21での変更点

- 辞書を1200語に拡張しました。
  - `dict_1001to1100.csv`: 銀行・お金、インターネット/SNS、乗り物、親戚関係、ペット、文房具、単位、家事、ビジネス動詞
  - `dict_1101to1200.csv`: 感情の名詞(勇気・不安・誇りなど)、社会・倫理・政治用語、雇用・労働関連
  - 引き続き、動詞には過去形・過去分詞列を含めて作成しています。
- 作成時の自己チェックで2件の重複を発見・修正済み(「再開する」の`resume`が既存の「履歴書」と重複、「署名する」の`sign`が既存の「標識」と重複)。

## v0.20での変更点

- 辞書を1000語に拡張しました。
  - `dict_701to800.csv`: 色、アクセサリー、キッチン用品、オフィス機器、感情の名詞、抽象概念、地理、犯罪・法律
  - `dict_801to900.csv`: 期間・頻度の表現(数年・数ヶ月など複合語)、ビジネス抽象名詞、科学・自然科学用語
  - `dict_901to1000.csv`: スポーツ・趣味、楽器、建物・場所、道路・建築部位、植物・庭関連、動物の体の部位
  - 701語以降は過去形・過去分詞列を最初から含めて作成しています。
- 作成時の自己チェックで3件の重複を発見・修正済み(「数」の`number`が既存の「数字」と重複、「約束(名詞)」の`promise`が「約束する(動詞)」と重複、「連絡(名詞)」の`contact`が「連絡する(動詞)」と重複)。また、入力ミス(910番の日本語列)と、実質機能しない見出し語(993番)も修正しました。

## v0.19での変更点(content.js同期)

このバージョンはユーザー側で直接`content.js`に加えられた変更を取り込んだものです。Kuromojiが「質問する」→「質問」+「する」、「数年」→「数」+「年」のように複合語を複数トークンに分割してしまう問題に対応するため、隣接する名詞・動詞トークン列を最長一致で辞書引きするロジックに一般化されています(v0.18で「名詞+する」専用に対応したものを、v0.19で複合語全般に拡張)。詳細はプロジェクト内の変更点まとめを参照。

## v0.17での変更点

- `dict_301to400.csv`〜`dict_601to700.csv`(301〜700語、残り4ファイル全て)に過去形・過去分詞列を追加しました。これで**1〜700語全範囲**で「過」バッジが表示されるようになっています。

## v0.16での変更点

- `dict_201to300.csv`(201〜300語)に過去形・過去分詞列を追加しました。これで1〜300語の範囲で「過」バッジが表示されるようになっています。301〜700語分はまだ未対応です。

## v0.15での変更点

- 「過」バッジの英単語リンクの文字色を、「類」と同じ青(#1a73e8)に変更しました。
- `dict_101to200.csv`(101〜200語)に過去形・過去分詞列を追加しました。これで1〜200語の範囲で「過」バッジが表示されるようになっています。201〜700語分はまだ未対応です。

## v0.14での変更点

- 「類」バッジの英単語リンクの下線を逆に変更: 通常時は下線なし、ホバー時に下線が表示されます(メインの英単語・過去形/過去分詞のリンクは従来どおり、通常時下線あり・ホバーで消える動作です)。
- `dict_001to100.csv`(1〜100語)に**過去形・過去分詞列を追加**しました。ヘッダー駆動パーサーのおかげで、ファイル名・他のCSVは変更していません。これにより、1〜100語の範囲では動詞の吹き出しに「過」バッジ(過去形・過去分詞)が実際に表示されるようになりました。101〜700語分はまだ未対応です。

## v0.13での変更点

- 吹き出し内の各英単語(リンク)の下線を、点線からストレートな下線に変更。ホバー時は下線が消えるようにしました(表示ロジックが点線下線とは逆になっています)。
- 類義語の英単語の文字色を青(#1a73e8)に変更し、リンクであることが視覚的に分かりやすいようにしました(メインの英単語・過去形/過去分詞の文字色は従来どおりです)。

## v0.12での変更点

- **CSVパーサーをヘッダー駆動方式に変更**しました。各CSVファイルの1行目(ヘッダー)を見て列の位置を判断するようになったため、今後「過去形」「過去分詞」のような新しい列を追加したいCSVが出てきても、そのファイルのヘッダーに列名を1つ足すだけで対応できます。既存の(その列を持たない)CSVファイルは一切変更不要のまま動作し続けます。
- 吹き出しに**過去形・過去分詞を表示する機能**を追加しました(データ側の対応はまだですが、機能自体は実装済みです)。表示条件は、
  - 品詞が動詞であること
  - 英単語が1語であること(「put out」のような熟語・句動詞は対象外)
  - CSVに「過去形」または「過去分詞」列の値が入っていること(現時点ではどのCSVにもこの列がないため、実際にはまだ何も表示されません)
  - 表示デザインは、類義語の「類」バッジと同じスタイルの「過」バッジを使用しています
- 過去形・過去分詞のデータそのもの(CSVへの列追加・値の記入)は、今回は見送っています。追加する際にご相談させてください。

## v0.11での変更点

- ルビの本文(日本語部分)に点線下線を追加。ホバー時は点線が消えるようにしました。
- 吹き出し内の英単語(メインの単語+類義語それぞれ)を、Google検索(`{単語} 英語 意味`)へのリンクにしました。外部リンクであることが分かるよう、小さなアイコンを併記しています。
- ※ 過去形・過去分詞の表示(4点目の要望)は、実装前に確認したい点があるため今回は見送っています。

## v0.10での変更点

- 辞書を700語に拡張(`dict_501to600.csv`: テクノロジー・抽象概念・動物・料理・買い物・自然・教育・スポーツ、`dict_601to700.csv`: 職業・衣類・天候・コミュニケーション動詞・方角/位置・季節行事・体調管理・ビジネス動詞)。
- 作成時の自己チェックで4件の重複を発見・修正済み(「試合」の`match`が「一致する」と重複、「教師」の`teacher`が「先生」と重複、「述べる」の`state`が「状態」と重複、「答える(650番)」が既存の「答える(68番)」と同一見出し語で重複)。

## v0.9での変更点

- アプリアイコンを設定しました(`icons/icon16.png` `icon32.png` `icon48.png` `icon128.png`)。ツールバー・拡張機能一覧に表示されます。
- 辞書を500語に拡張(`dict_words/dict_401to500.csv`を追加、`dict_manifest.js`に登録済み)。

## v0.8での変更点(重要な構造変更)

- **アプリ名を「Rooby_JP」に変更しました。**「JP」を付けたのは、辞書CSV内の日本語部分を別の言語に差し替えれば将来的に他言語対応も見据えられる、という位置づけを明確にするためです(今回、多言語対応の実装自体はしていません)。
- **辞書データの管理方法を全面的に変更しました。** これまでの「CSVをPythonでマージして`dict.js`という1つの生成物を作る」方式をやめ、**CSVファイルを直接拡張機能に読み込ませる**方式に切り替えました。
- 辞書CSVは `dict_words/` フォルダの中に、`dict_001to100.csv`, `dict_101to200.csv`, `dict_201to300.csv`, `dict_301to400.csv` という形でbatch単位のまま格納されています。今後新しいbatchを追加する際は、
  1. `dict_words/` フォルダに新しいCSVを追加する
  2. `dict_manifest.js` の `DICT_FILES` 配列に1行追記する

  の2手順だけで反映されます(`dict.js`のような統合ファイルを作り直す必要がなくなりました)。
- 新規ファイル `dict_loader.js`: CSVを取得(fetch)してパースする共通ロジック。`content.js`と`options.js`の両方から使われます。
- 設定画面(options.html)の単語一覧に、**優先度番号(`#101`のような形)を表示**するようにしました。検索ボックスに番号を入力しても該当単語を検索できます。
- 上記の変更に伴い、`content.js`の初期化処理が非同期(CSV読み込み待ち)になっています。

### 今後の展望(今回は未実装)
将来的に語彙が2000語・4000語・6000語と増えていった際、「0001〜2000語のみ有効化」のような**単位ごとのチェックボックス切り替え機能**を追加する構想があります。今回の`DICT_FILES`配列によるファイル管理は、この機能を見据えた設計になっています(実際の切り替えUIは、語彙数が増えてきたタイミングで別途実装予定です)。

## v0.7での変更点(→ v0.8で破棄・ロールバック済み)

v0.7では、ルビの配置方式をネイティブ`<ruby>`から`position: absolute`によるローカル絶対配置に変更していましたが、前の行との重なり・`overflow: hidden`での見切れが再発する懸念があったため、**v0.8ではv0.6のネイティブ`<ruby>`方式に戻しています**(以下の「v0.7での変更点」の内容は現在のコードには含まれていません)。

- ルビの配置方式を、ネイティブ`<ruby>`レイアウトから**`position: absolute`によるローカルな絶対配置**に変更(`ruby`要素に`position: relative`、`rt`要素に`position: absolute; left: 0; top: -1em;`)。
  - 単語の上に余白(`margin-top: 1.1em`)を追加し、前の行の文字と重ならないようにしています
  - **注意**: この方式は、親要素に`overflow: hidden`が設定されているサイトでは、はみ出た部分が見切れる可能性が再び生じます(v0.4でネイティブrubyに切り替えた際に一度解消した問題です)。見切れが頻発するようであれば、また別の対策を検討する必要があります

## v0.6での変更点

- アプリ名を「rooby」に変更しました。
- 辞書をbatch1(100語)+batch2(100語)で**合計200語**に拡張しました。
- **設定画面(オプションページ)を追加**。ツールバーのポップアップ内「単語ごとの詳細設定を開く」から開けます。
  - 検索ボックスで日本語・英語・カタカナ・類義語のいずれでも部分一致検索できます
  - 単語ごとにチェックボックスでルビ表示のON/OFFを個別に切り替えられます(例: 「なる」「いる」だけ非表示にする、など)
  - 「表示中を全てON/OFF」ボタンで、検索結果に対する一括操作も可能です
  - 設定は`chrome.storage.local`に保存され、次回のページ読み込みから反映されます
- ルビの見た目を変更: フォントサイズ`10px`、太字、イタリック解除、斜め回転を廃止して通常のふりがなと同じ形に、左寄せ(`ruby-align: start`)。

## ⚠️ v0.4を使う前に必須の作業

このZIPには **Kuromoji.js本体と辞書データが含まれていません**(容量の都合上)。以下の手順で追加してください。

1. https://github.com/takuyaa/kuromoji.js を開き、「Code」→「Download ZIP」でダウンロード
2. 解凍してできたフォルダの中から、次の2つをこの `ext` フォルダに配置する
   - `build/kuromoji.js` → `ext/kuromoji.js` としてコピー(ファイル名はそのまま、場所だけ `ext` 直下に)
   - `dict/` フォルダ(中に `.dat.gz` ファイルが複数入っている、合計10MB程度) → まるごと `ext/dict/` としてコピー
3. 最終的に `ext` フォルダの中身が以下のようになっていればOKです

```
ext/
  kuromoji.js       ← 追加(Kuromoji.js本体)
  dict/             ← 追加(辞書データ一式、.dat.gzファイル)
  manifest.json
  content.js
  content.css
  dict.js
  popup.html
  popup.js
  test.html
  README.md
```

配置が終わったら、`chrome://extensions` で読み込み(または既存のものを「更新」)してください。

## v0.4での変更点

- **Kuromoji(形態素解析)を導入**。これまでの単純な文字列マッチングをやめ、正しく単語の境界を認識するようにしました。「重なる」の中の「なる」に誤反応する、といった問題が解消されます。
- **「名詞+する」の「する」を除外する設定を追加**(デフォルトON)。「説明する」のような複合動詞で「する」単体にdoのラベルが付かないようになります。ツールバーのポップアップからON/OFF可能(この設定はページ再読み込み後に反映されます)。
- **ラベル表示をネイティブの`<ruby>``<rt>`タグに変更**。これまでの「単語の上に浮かせて絶対配置する」方式をやめ、ブラウザ標準のふりがな表示機構に乗せることで、文字が見切れる問題やパフォーマンスの懸念を構造的に解消しました。
  - 注意点: サイト側がもともと`<ruby>`タグを使っている場合(教育系サイトなど)、サイト側のCSSと干渉する可能性があります。`.rbv-ruby`というクラス名と`!important`で防御していますが、100%ではありません。
- ラベルのフォントサイズを`6px`固定、太字を解除、下に3pxオフセット、`-20deg`回転(起点は左下)。
- aタグ内の単語をクリックした場合、常にリンク遷移をキャンセルして吹き出しを表示するようにしました。
- 吹き出しに英単語自体も表示するようにしました(これまではカタカナ・類義語のみ)。
- ツールバーのポップアップに「英語 / カタカナ」の表示切り替えを追加(ラベルに英単語を出すか、カタカナ読みを出すかを一括切り替え。即時反映)。

## v0.3での変更点

- ラベルの角度を `-20deg` に固定(単語ごとのランダムな角度変化は廃止)。`transform-origin` を単語の左下に統一し、揃った見た目になるようにした。
- `span` ではなく未知のカスタムタグ(`<rbv-word>` `<rbv-label>` `<rbv-jp>`)を使用し、`all: revert` でリセットすることで、サイト側の `p span {...}` のような要素セレクタからの巻き込みを回避。
- オレンジの英単語ラベル自体もクリック可能に(`pointer-events: none` を削除)。
- ラベル・単語の `z-index` を高く設定し、他要素の背面に隠れる問題に対応。
- 吹き出しに ✕ボタンを追加。同じ単語を再クリックすると吹き出しがトグルで閉じる。別の単語をクリックすると吹き出しが切り替わる。

## v0.2での変更点

- ラベル(英単語)を単語ごとに角度・高さをランダム風にずらして斜め表示。単語が連続しても重なりにくくなりました。
  (同じ単語は常に同じ角度・高さになるよう決定論的に計算しているので、表示の一貫性は保たれます)
- クリック時のポップアップから日本語の見出し語表示を削除(文中にすでに書いてあるため不要、というフィードバックを反映)。カタカナ読みと類義語のみ表示。
- ツールバーのアイコンをクリックするとON/OFFを切り替えられるようになりました。切り替えはページ再読み込み不要で即座に反映されます(設定はChromeのstorageに保存され、ブラウザを閉じても保持されます)。

## 制限事項(プロトタイプ版)

- Kuromoji(形態素解析)は未使用。辞書の見出し語(辞書形)との**文字列マッチング**のみ。
  そのため「見た」「思った」のような活用形にはマッチしません(「見る」「思う」など辞書形のみ反応します)。
- 辞書は100語のみ(vocab_batch1_v3.csv 由来)。
- カタカナ読みは仮の精度です。

## インストール方法

1. Chromeで `chrome://extensions` を開く
2. 右上の「デベロッパーモード」をONにする
3. 「パッケージ化されていない拡張機能を読み込む」をクリック
4. このフォルダ(ext)を選択する

## 動作確認方法

### 方法A: 同梱のテストページを使う
1. `test.html` をダブルクリックしてブラウザで開く(file:// で開かれます)
2. **file:// のページでも拡張機能を動かすには**、`chrome://extensions` の該当拡張機能の「詳細」を開き、
   「ファイルのURLへのアクセスを許可する」をONにしてください。これをやらないと file:// では動きません。

### 方法B: 適当なニュースサイトなどで試す
- 拡張機能インストール後、任意の日本語ニュースサイトを開くだけで動作します(https/httpページなら追加設定不要)。
- ただし辞書が100語しかないため、マッチする単語がないと何も表示されません。試すなら「政府」「発表」「会社」
  「制度」などの単語が含まれる記事がおすすめです。

## ファイル構成

- `manifest.json` : Chrome拡張の設定(Manifest V3)
- `content.js` : ページ内テキストを走査し、ルビ表示・クリックイベントを付与するメインロジック
- `content.css` : ルビ表示・吹き出しのスタイル
- `dict.js` : 辞書データ(vocab_batch1_v3.csv から自動生成)
- `test.html` : 動作確認用のサンプル記事

---

## アップデート告知機能の運用方法(開発者向け)

新しいバージョンをリリースしたら、リポジトリ直下の`latest.json`を以下の形式で更新すること。
ユーザーがポップアップを開いた時に、この内容と手元のバージョンを比較して、古ければお知らせバナーが表示される。

```json
{
  "latest": "0.xx",
  "url": "https://github.com/YuukiKws/roobee"
}
```
